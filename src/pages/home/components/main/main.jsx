import { Card } from "../card/card"
import { Section } from "../section/section"
import "./main.css"

export const Main = () => {
    return (
        <main>
            <Section/>
            <Card/>
        </main>
    )
}